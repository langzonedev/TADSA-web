# Device implementation status

The earlier independent starter is superseded by shared UI and an IndexedDB API adapter. Implemented: persistent migration, atomic versions/retries, intake/contacts/projects/people, notes/funding/drafts/receipts, approval gates, calendar/areas, NDIS/location/labour, files and reproducible offline shell.

Remaining: backup restore UI, mobile installation/update/eviction checks, broader browser compatibility and growing API contract tests. Real accounts, server sync, live integrations, Access migration and production deployment are outside this demonstration.

Run README suites and regenerate shell hashes after changes. Record executed evidence separately from proposed checks.
