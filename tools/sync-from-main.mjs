// Explicit export allowlist: never recursively copy the private source repository.
import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {resolve,dirname} from 'node:path';
import {fileURLToPath} from 'node:url';
import {createHash} from 'node:crypto';
import {execFileSync} from 'node:child_process';
const source=process.argv[2];if(!source)throw Error('Usage: node tools/sync-from-main.mjs <main-repository-path>');
const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'),main=resolve(source);
const files=['contact-draft.js','record-values.js','management-report.js','invoice-references.js','invoice-references-model.js','profile-details.js','profile-details-model.js','project-details.js','project-details-model.js','lifecycle-model.js','project-lifecycle.js','finance-pack.js','technician-credentials.js','accounts.js','reports.js','report-model.js','report-builder.js','jpeg.js','app.js','settings.js','appearance.js','workflows.js','operations.js','client-details.js','local-login.js','availability.js','technician-location.js','project-care.js','styles.css','boot.js','assets/tadsa-logo.png'];
const manifest={sourceRevision:execFileSync('git',['-C',main,'rev-parse','HEAD'],{encoding:'utf8'}).trim(),note:'Content hashes are authoritative; source working tree may contain the current uncommitted integration.',files:{}};
const hash=b=>createHash('sha256').update(b).digest('hex');
for(const path of files){let data;try{data=await readFile(resolve(main,'public',path));}catch(e){if(['availability.js','technician-location.js','project-care.js'].includes(path)&&e.code==='ENOENT')continue;throw e;}await mkdir(dirname(resolve(root,path)),{recursive:true});await writeFile(resolve(root,path),data);manifest.files[path]={source:'public/'+path,sha256:hash(data),transform:null};}
const seed=await readFile(resolve(main,'src/seed.mjs'));await writeFile(resolve(root,'seed.mjs'),seed);manifest.files['seed.mjs']={source:'src/seed.mjs',sha256:hash(seed),transform:'Fictional authored fixture only'};
const enrichmentSource=await readFile(resolve(main,'src/seed-enrichment.mjs'),'utf8');const enrichment=enrichmentSource.replaceAll('../public/','./');await writeFile(resolve(root,'seed-enrichment.mjs'),enrichment);manifest.files['seed-enrichment.mjs']={source:'src/seed-enrichment.mjs',sourceSha256:hash(enrichmentSource),sha256:hash(enrichment),transform:'Use exported shared model paths'};
const indexSource=await readFile(resolve(main,'public/index.html'),'utf8');const index=indexSource.replace('<script defer src="./boot.js"></script>','<link rel="manifest" href="./manifest.webmanifest"><meta name="theme-color" content="#2f3e84"><script type="module" src="./pwa-start.mjs"></script>').replace(/This local workspace saves changes to PostgreSQL\.[^<]*/, 'This device demonstration stores fictional records in this browser only. Published organisation and issuer details may be real. Demo credentials simulate sign-in on this device. No server synchronisation or live payment processing. Do not enter real information.');if(index.includes('saves changes to PostgreSQL')||!index.includes('This device demonstration stores fictional records'))throw Error('Device disclosure transformation did not match source HTML.');await writeFile(resolve(root,'index.html'),index);manifest.files['index.html']={source:'public/index.html',sourceSha256:hash(indexSource),sha256:hash(index),transform:'Replace boot entry with device adapter and PWA metadata; truthful device storage disclosure'};
await writeFile(resolve(root,'SHARED_FILES.json'),JSON.stringify(manifest,null,2)+'\n');
const shell=['./','./index.html','./pwa-start.mjs','./device-api.mjs','./device-invoice-references.mjs','./device-profile-details.mjs','./device-project-details.mjs','./device-model.mjs','./device-reports.mjs','./device-extensions.mjs','./device-files.mjs','./device-tools.mjs','./device-iteration.mjs','./device-business.mjs','./device-backups.mjs','./device-accounts.mjs','./device-credentials.mjs','./device-lifecycle.mjs','./device-lifecycle-validation.mjs','./seed.mjs','./manifest.webmanifest','./icon.svg',...Object.keys(manifest.files).filter(p=>!['index.html','seed.mjs','boot.js'].includes(p)).map(p=>'./'+p)];
const shellHashes=await Promise.all(shell.filter(p=>p!=='./').map(async path=>({path,sha256:hash(await readFile(resolve(root,path)))})));
manifest.shellHash=hash(JSON.stringify(shellHashes));
await writeFile(resolve(root,'SHARED_FILES.json'),JSON.stringify(manifest,null,2)+'\n');
await writeFile(resolve(root,'sw.js'),`// Generated by tools/sync-from-main.mjs. Only this application's shell is cached.\nconst CACHE='tadsa-shell-${manifest.shellHash.slice(0,12)}';\nconst SHELL=${JSON.stringify(shell)};\nself.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(SHELL))));\nself.addEventListener('message',e=>{if(e.data==='ACTIVATE_UPDATE')self.skipWaiting();});\nself.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('tadsa-shell-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));\nself.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url),base=new URL(self.registration.scope);if(u.origin!==base.origin||!u.pathname.startsWith(base.pathname))return;const allowed=SHELL.some(p=>new URL(p,base).pathname===u.pathname);if(!allowed&&e.request.mode!=='navigate')return;e.respondWith(caches.open(CACHE).then(async cache=>(await cache.match(e.request))??(e.request.mode==='navigate'?await cache.match('./index.html'):null)??fetch(e.request)));});\n`);
console.log('Synchronized '+Object.keys(manifest.files).length+' explicitly allowed files.');
